galaxy_info:
  author: Leandro Ramos
  description: Ansible role to deploy new Debian servers
  company: PyCafe

  license: GPL-3.0-or-later

  min_ansible_version: 2.1

  platforms:
    - name: Debian
      versions:
        - bullseye
        - buster
        - stretch
  
  galaxy_tags:
    - debian
    - server
    - packages
    - configs
    - bootstrap

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
